# AssetLoop / 物迹 项目计划

图片增量：在现有型号配置中维护 PNG/JPEG/WebP 图片，支持上传、替换、解除展示关联
和服务器公开 HTTPS 直链导入；详情页无可用 3D 时回退到图片，列表显示缩略图。
与通用附件、3D 资源和生命周期账务保持独立。暂不包含图片专用 MCP 工具或
以旧换新关联；后者需另行确定事件引用与成本口径，不能重复记一笔卖出收入。

状态：阶段 1、2A、2B、2C 已完成；核心 MCP、GLB 导入和型号图片已验收进入 UAT e8ab8f9。
本次开发整合接通二手行情、每日采集与 MCP 行情工具，通用附件及行情曲线 UI 仍待完成。
规格标签化和 00013–00014 升级的历史证据保留于 docs/specification-acceptance.md；
最新批次以对应提交的双数据库 CI 和全要素结果为准。
最后更新：2026-09-12

2026-09-07 管理抽屉统一：类别、型号、标签类型／值、生命周期类型、3D 上传及资源预览编辑统一使用紧凑固定标题栏，右侧文字取消／保存（上传保留操作名称），取消兼作关闭，正文独立滚动。型号配置及资源信息／标签分别统一事务保存；校验失败保留草稿，整次修改原子回滚。共享标签仅在实际改名且已有引用时提示引用数并确认保存，不再常驻确认框。成员管理仍保留页面形态。

本文只管理产品范围和实施阶段。非协商边界以 `../AGENTS.md` 为准，系统结构以 `ARCHITECTURE.md` 为准，当前路径导航以 `../CODEMAP.md` 为准。

## 1. 项目目标

构建一个自用优先、可开源、可平滑演进为 SaaS 的物品生命周期与持有成本系统。

用户通过 AI 对话提交订单、商品、价格或维修凭证截图。AI Harness 负责识别、补全并在对话中逐项确认信息，随后通过语义化 MCP 工具直接写入正式生命周期。Web 页面负责维护与追加式更正物品记录，以及查看二手行情、持有成本和生命周期。

Web 的主要用户入口是具体物品列表。类别归属型号，型号维护允许选择的类型化标签与外观默认；物品直接选择型号和实际标签。录入时可就地补充类别与型号，标签字典独立复用。未来 Harness/MCP 复用同一应用用例，不维护第二套规格树。

物品列表搜索停止输入 300ms 后局部更新，高级筛选点击应用后生效；排序、分页和列表/卡片切换也只更新结果区及相关计数、导航状态。保留搜索焦点和页面位置，失败保留原结果并可重试，无脚本时回退原生 GET。

系统应做到：

- 同一份业务代码支持本地单用户版和未来 SaaS。
- 默认使用 SQLite，同时正式适配 PostgreSQL。
- 默认将附件保存在本地，同时适配阿里云 OSS。
- 转转是首个行情数据源，通过接口边界保持可替换。
- 所有统计统一使用租户本位币，原始货币信息可追溯。
- 应用升级时自动、安全地升级旧数据结构，不破坏已有数据。

## 2. 已确认的产品边界

### 2.1 第一阶段包含

- Agent 对话确认后的语义化 MCP 直接入库流程。
- 物品类别、型号、类型化规格标签、具体物品和生命周期管理。
- 买入、卖出、维修及其他资产事件。
- 原币、本位币和汇率留痕。
- 转转二手成交参考价、共享物品绑定和每日快照。
- 二手价格曲线、当前估值、持有成本和日均减值展示。
- Web 管理页面。
- 本地账号登录、租户成员和 Owner/Editor/Viewer 权限。
- 面向 AI Harness 的语义化 MCP 工具。
- SQLite/PostgreSQL、Local/OSS 的兼容层。
- 独立 3D 资源库、型号通用默认、已确认外观默认及具体物品专属模型，支持手工维护与详情交互展示。
- 跨平台单二进制发布。

### 2.2 第一阶段不包含

- 应用内置大模型调用和模型 API Key 管理。
- 裸 SQL MCP 工具。
- Go `.so` 动态插件。
- 对任意数据库的名义兼容。
- 第三方数据源插件市场。
- 复杂会计总账或企业 ERP 功能。

## 3. 总体架构

```text
AI Harness / Browser / Scheduler
              |
       Web + MCP + CLI
              |
      Application Services
       /        |         \
    Store    BlobStore   MarketProvider
    /  \       /  \          /   \
SQLite PG   Local OSS    Zhuanzhuan FX
```

依赖方向固定为：入口层调用应用服务，应用服务调用领域接口，基础设施适配器实现接口。领域层不得依赖具体数据库、阿里云 SDK 或 OneBound 字段。

## 4. 技术选型

### 4.1 后端

- 语言：Go。
- HTTP：标准库 `net/http`。
- HTML：标准库 `html/template`，服务端渲染。
- 局部交互：HTMX，静态资源嵌入二进制。
- 图表：Chart.js，按需引入。
- 日志：标准库 `log/slog`。
- 认证：本地密码哈希、服务端不透明 Session 和表单 CSRF；SaaS 身份源后续接入。
- 数据库访问：`database/sql`。
- SQL 代码生成：sqlc；生成代码提交到 Git。
- 数据迁移：Goose，迁移文件嵌入二进制。
- SQLite 驱动：纯 Go 的 `modernc.org/sqlite`。
- PostgreSQL 驱动：`pgx/v5/stdlib`。
- MCP：Go MCP SDK，提供业务语义工具。

不引入运行时 ORM、React、Node 构建链或微服务拆分。

产品 3D 查看使用随二进制静态打包的同版本 Three.js、GLTFLoader 和 OrbitControls，不依赖 CDN 或部署时 Node 环境。

### 4.2 分发

同一个可执行程序提供子命令：

```text
assetloop serve
assetloop mcp
assetloop migrate
assetloop refresh
assetloop install-scheduler
```

通过 CI 构建 Windows、Linux、macOS 的独立二进制和可选 Docker 镜像。

## 5. 领域模型

### 5.1 核心层级

```text
Tenant
└── ItemCategory
    └── ProductModel
        ├── 允许标签 / 外观默认
        └── Asset
            ├── 实际选择的标签
            └── AssetEvent
```

- `ItemCategory`：手机、相机、耳机等类别，不预置固定类别。
- `ProductModel`：如 iPhone 17 Pro。
- 标签类型和值：颜色、容量、内存等可复用描述；型号限定可选范围，全部维度可留空。
- `Asset`：直接归属型号的具体物品，保存实际标签选择、别名、序列号、备注和购买渠道。
- `AssetEvent`：买入、卖出、维修、退货、升级、状态变更等生命周期事件。

颜色属于类型化标签，默认参与外观匹配；容量和内存默认不参与。型号可以覆盖维度的外观属性。不校验厂商销售组合，不把产品型号和类别改成标签。用户已明确取消旧规格兼容：最终只保留型号与标签，删除旧规格表、字段、维护接口及输入转换。

### 5.2 分类和成色

- AI 可以建议创建新类别，但必须由用户确认。
- 已有类别内，信息完整且用户确认后，Harness 可维护型号和类型化标签；组合的产品知识判断不放入应用核心。
- 信息不完整时先补充检索或询问用户。
- 每个类别可以维护自己的成色等级和映射规则。

### 5.3 交易与事件

- `transactions` 表示一次业务交易，可关联多个物品事件。
- `asset_events` 是物品生命周期的事实来源。
- 租户可以就地新增事件类型，并明确该类型属于支出、收入或无金额；无金额事件不锁定本位币，也不改变内置持有状态。
- 生命周期类型管理支持分页搜索、改名、停用与恢复；内置类型只读，自定义类型一旦被记录引用便锁定收支方向。事件通过类型 ID 读取当前名称，改名不复制更新历史记录；停用保留历史查询及原记录更正能力。Owner/Editor 管理，Viewer 只读，旧名称参数保留兼容解析。
- 金额使用最小货币单位的有符号整数：支出为负，收入为正。
- 已确认事件不直接覆盖；更正采用作废并创建替代记录，以保留审计轨迹。

## 6. 金额、本位币和汇率

### 6.1 本位币

- 本地版自动创建一个默认租户。
- “系统全局本位币”在数据模型中是租户级全局设置。
- 默认本位币为 CNY。
- 第一条金额记录写入后，本位币锁定，普通设置操作不能直接修改。
- 系统内资产估值、收益、成本和报表全部按本位币计算。

### 6.2 非本位币入库

当原始货币不同于本位币时：

1. 用户确认采用哪一天的汇率。
2. 默认采用当前日期汇率。
3. 计算本位币金额并落库。
4. 同时保留原始金额、原始货币、汇率、汇率日期和来源。

建议字段：

```text
amount_base_minor
base_currency
amount_original_minor nullable
original_currency nullable
fx_rate nullable
fx_rate_date nullable
fx_rate_source nullable
```

自动行情点可按行情日期自动取得汇率，但仍必须保存换算证据。

### 6.3 币种目录

- 本位币设置、生命周期事件表单和金额解析共用一份随代码发布的 ISO 4217 当前币种目录。
- 目录固定记录来源版本，运行时不依赖外网；新增记录只能选择当前有效币种。
- 币种退役后保留其历史解析定义，但不再允许用于新的本位币或事件。

## 7. 数据库兼容层

### 7.1 正式支持范围

- 默认：SQLite。
- SaaS：PostgreSQL。
- MySQL 仅保留未来适配位置，当前不承诺支持。

业务层使用按业务用例定义的 `Store` 接口，不暴露通用表级 CRUD：

```go
type Store interface {
    AppendAssetEvent(ctx context.Context, cmd AppendAssetEvent) error
    ListAssets(ctx context.Context, query AssetQuery) ([]Asset, error)
    SaveMarketSnapshot(ctx context.Context, snapshot MarketSnapshot) error
    ListDuePriceSeries(ctx context.Context, now time.Time) ([]PriceSeries, error)
}
```

SQLite 和 PostgreSQL 分别维护查询 SQL 和 sqlc 生成包。事务边界由具体 Store 操作封装。

### 7.2 SaaS 数据边界

- 所有业务表从第一版起包含 `tenant_id`。
- 所有唯一约束包含 `tenant_id`。
- 应用服务显式接收租户 ID。
- ID 由应用生成 UUID，不依赖数据库自增。
- 金额使用 `BIGINT` 最小货币单位。
- 时间统一为 UTC。
- 核心逻辑避免依赖触发器、存储过程和数据库特有生成列。

### 7.3 用户和租户权限

- `users` 保存全局登录身份，`tenant_memberships` 保存租户角色。
- 第一版固定 `owner`、`editor`、`viewer` 三个角色，不提供自定义权限编辑器。
- Web 从 Session 解析用户和租户；浏览器提交的 `tenant_id` 不作为授权依据。
- Application 服务检查能力，Store 仍在每条业务查询中强制 `tenant_id`。
- 平台运维权限与租户角色分离，第一版不提供跨租户数据浏览后台。
- 本地认证只能在 loopback 监听地址上关闭。

## 8. 数据迁移和版本升级

迁移按数据库方言分开，但保持相同逻辑版本号：

```text
migrations/
├── sqlite/
│   ├── 00001_initial.sql
│   └── 00002_add_original_currency.sql
└── postgres/
    ├── 00001_initial.sql
    └── 00002_add_original_currency.sql
```

### 8.1 SQLite 升级

应用启动时：

1. 获取跨进程升级锁。
2. 检查程序支持的 schema 版本。
3. 仅在已有结构需要升级时，使用 SQLite `VACUUM INTO` 创建一致的升级前备份并验证完整性。
4. 只执行向前迁移。
5. 运行完整性和外键检查。
6. 成功后启动；失败则停止并保留备份。

旧版程序遇到更高版本数据库时必须拒绝启动。

### 8.2 PostgreSQL 升级

- 由发布流水线中的单独迁移任务执行。
- 使用 PostgreSQL advisory lock 避免多个实例同时迁移。
- 应用实例启动只校验版本，不自行修改生产表结构。
- 采用 expand-contract：先增加兼容结构，再回填和切换，后续版本才删除旧结构。
- 生产环境不依赖 destructive down，修复使用新的向前迁移。

### 8.3 CI 验证

- 两种数据库都从空库建库。
- 两种数据库都运行同一套 Store 一致性测试。
- 每个新功能在同一次变更中增加最小自动回归测试。
- 持续扩展唯一命名的 `TestFullElementScenario`，覆盖所有已交付纵向业务能力。
- UAT 打包前必须分别以 SQLite 和 PostgreSQL 运行全要素场景，不允许跳过 PostgreSQL。
- 保存上一正式版本数据库样本并执行升级测试。
- 检查同一迁移版本是否同时存在于两个方言目录。
- 检查 sqlc 生成代码没有过期。

## 9. 附件存储兼容层

### 9.1 支持范围

- 默认附件存储：本地文件系统。
- OSS 实现：默认阿里云 OSS。
- Bucket 默认私有。

应用服务只调用 `BlobStore`：

```go
type BlobStore interface {
    Put(ctx context.Context, key string, body io.Reader, metadata BlobMetadata) (BlobInfo, error)
    Open(ctx context.Context, key string) (io.ReadCloser, error)
    Stat(ctx context.Context, key string) (BlobInfo, error)
    Delete(ctx context.Context, key string) error
}
```

首批实现：

```text
LocalBlobStore
AliyunOSSBlobStore
```

### 9.2 统一对象键映射

本地和 OSS 共用唯一的 `ObjectKeyMapper`：

```text
tenants/{tenant_id}/attachments/{yyyy}/{mm}/{attachment_id}/original.{ext}
tenants/{tenant_id}/attachments/{yyyy}/{mm}/{attachment_id}/thumbnail.webp
tenants/{tenant_id}/models/{product_model_id}/{sha256}.glb
```

同一个对象键可以映射为：

```text
本地：{local_root}/{object_key}
OSS： oss://{bucket}/{prefix}/{object_key}
```

业务表不保存本地绝对路径、Bucket 域名或临时签名 URL。

### 9.3 附件元数据

```text
attachments
- id
- tenant_id
- owner_type
- owner_id
- purpose
- store_id
- object_key
- original_filename
- mime_type
- byte_size
- sha256
- status
- created_at
- deleted_at
```

`store_id` 记录附件实际所在位置。修改默认存储只影响新附件，历史附件仍从原 Store 读取。

本地迁移到 OSS 时使用相同对象键：复制文件、校验大小和 SHA-256、更新 `store_id`，最后才允许清理旧文件。

### 9.4 安全要求

- 限制文件大小和允许的 MIME 类型。
- 不使用原始文件名拼接对象路径。
- 防止路径穿越。
- OSS 密钥不得写入数据库、日志或 Git。
- 不保存永久公开 URL。
- 下载经应用鉴权后代理，或生成短时签名 URL。

## 10. 行情数据源兼容层

本期采用转转最新一期成交行情，详细契约及接入证据见 [market-integration.md](market-integration.md)。

- MarketDataProvider.FetchQuote 返回型号描述、成交区间、提供方版本与脱敏证据；转转适配器使用远程 MCP，核心无需模型 API。
- 应用层固定选 dealMaxPrice，口径为“提供方最新一期成交最高价”。不做挂牌聚合、中位数、异常值或置信度评分。
- market_items 保存租户、显示名称、不可变来源/关键词/配置条件/地区、匹配型号、可空外部 ID 及刷新状态。转转没有稳定外部型号 ID，不用商品链接冒充。
- asset_market_bindings 提供租户内显式多对一关联，避免重建既有 assets 表。不同配置必须独立创建；更改查询创建新序列，改名不影响历史。
- market_prices 按北京时间采集日保留最后一次成功快照，整数分保存 CNY 原价与可空本位币金额，附实际采集时间、汇率日期/来源、计算口径和证据。缺失统计日期与样本量留空。
- 币种及原始金额单位由各平台适配器确定：转转默认人民币、元，适配器转为整数分。应用层按返回的 ISO 币种与最小单位处理，不设置用户确认开关。
- Frankfurter v2 默认合成日汇率自动换算，实际日期不晚于采集日；人民币直接使用。缺汇率仍保存原价，后续按原采集日期补算，首次行情金额锁定本位币。
- 来源令牌只经环境或忽略的本地配置加载，界面只显示配置状态。闲鱼、OneBound、导入及商品图片延期。

## 11. 行情曲线和估值

### 物品成本看板（不依赖行情）

- 物品详情下半区采用成本看板与右侧约 280px 紧凑时间线，窄屏上下排列。四项数字为日均持有成本、净成本、持有天数与累计支出；净成本展示支出减回收的拆解。
- 净成本包含全部有效收入/支出，排除作废及技术作废事件；日均以本地日历日期从买入至今日或卖出日（含首日）计算，精确除法按币种最小单位四舍五入。负值保留并说明回收超过支出；缺少买入或异常日期不显示日均。卖出后的费用、退款仍更新成本，不延长天数。
- 阶梯图展示事件累计净成本，支出按事件类型分组；明细表提供可访问的图表替代。单一类别不绘制装饰性条形图。列表筛选、排序和分页不影响完整资产看板。
- 时间线搜索停止输入 300ms 后局部更新；高级筛选统一点击应用后生效，草稿条件不夹带进搜索或分页。面板支持外部点击与 Escape 关闭；仅替换记录及分页，保留输入焦点、3D 与成本看板，失败保留旧结果并重试，无脚本时回退原生 GET 表单。
- 时间线默认只显示日期、类型图标、名称、金额；悬停 150ms 或聚焦使记录本身轻微放大，备注、汇率证据与更正入口在同一条记录内部展开，不使用分离弹出框。展开/收起用 240ms 网格过渡保持连续性；点击固定，外部点击/Escape 关闭。窄屏取消缩放，减少动态效果模式取消位移；收起内容不可聚焦。
- 成本看板继续只读真实生命周期现金流；行情参考区独立展示来源图标、价格和获取时间，不增加假设卖出成本。

### 本期行情能力

- 设置中的“二手行情”列表与编辑抽屉支持商品搜索、候选规格核对、查询预览、确认创建、改名、停用和手动刷新。默认先选商品，保留直接按型号查询。
- search 返回候选列表，选中后才查询 product_detail；规格缺失显示未提供。确认页区分商品规格与行情匹配范围，明确接受后才保存。商品在售价仅辅助选择，不写入行情价格表。
- 保存前重新核对商品规格及行情型号；变化后重新确认。00016 保存可空的不可变规格选择快照；相同查询复用原快照，每日刷新不依赖商品仍在售。
- 列表筛选仅查找已添加行情的名称和匹配型号，空列表隐藏筛选栏；外部行情查询位于新增抽屉。新增、筛选、编辑、刷新使用有可访问名称及悬停提示的图标。
- 物品编辑选择已有行情，或以型号与标签预填查询后创建并选择；不会按型号自动绑定。
- 详情首版仅显示转转图标、最新二手参考价、实际获取时间。失败保留旧价格和时间；没有数据展示“暂无二手价格”。外币空间缺汇率时显示人民币原价。
- 每天北京时间 09:00 由系统计划任务运行 assetloop refresh-market，应用启动时补采当前日。不会伪造关机期间历史。
- 按共享二手物品去重采集，同日更新最后成功快照；串行限速、有限重试、数据库租约，鉴权失败停止本批。
- 所有关联设备卖出后继续采集 90 天，再停止；绑定新持有设备后恢复。停用保留历史。
- 历史曲线界面、额外估值指标留待后续。

## 12. 配置管理

### 12.1 部署配置

本地版使用 `.env`；仓库只提交 `.env.example`，真实 `.env` 必须加入 `.gitignore`。

生产或 SaaS 可以直接注入真实环境变量和 Secret Manager，不要求磁盘上存在 `.env`。

配置优先级：

```text
真实环境变量
→ 指定的 .env 文件
→ 默认位置的 .env
→ 程序默认值
```

示例：

```dotenv
APP_ENV=local
HTTP_ADDR=127.0.0.1:8080
LOG_LEVEL=info

DB_DRIVER=sqlite
DB_DSN=./data/assetloop.db

ATTACHMENT_DEFAULT_STORE=local
ATTACHMENT_LOCAL_ROOT=./data/blobs

OSS_PROVIDER=aliyun
ALIYUN_OSS_ENDPOINT=https://oss-cn-shanghai.aliyuncs.com
ALIYUN_OSS_REGION=cn-shanghai
ALIYUN_OSS_BUCKET=assetloop
ALIYUN_OSS_ACCESS_KEY_ID=
ALIYUN_OSS_ACCESS_KEY_SECRET=
ALIYUN_OSS_PATH_PREFIX=

ONEBOUND_ENABLED=false
ONEBOUND_BASE_URL=
ONEBOUND_APP_KEY=
ONEBOUND_APP_SECRET=
```

### 12.2 业务配置

以下配置保存在数据库而不是 `.env`：

- 本位币及锁定状态。
- 行情数据源启用状态和优先级。
- 默认市场地区。
- 行情刷新周期。
- 分类成色等级。
- 估值统计口径。

本地版属于默认租户设置；SaaS 版属于各自租户设置。

## 13. Web 和 MCP

### 13.1 Web 页面

物品列表和卡片标题优先使用用户自定义名称，未填写时使用类型（型号）名；自定义名称为选填，不拼接规格作为默认名称。分类和规格在次要文字中展示；标题已经是型号名时，次要文字不再重复型号，使用不同自定义名称时才补充型号。序列号仅在详情和编辑页展示，仍可用于搜索。长标题最多展示两行，详情保留完整名称。

- 物品、型号、成员和生命周期记录列表；统一使用服务端筛选、白名单排序和分页，并批量加载关联数据以避免 N+1 查询。
- 类别、型号、类型化标签和型号允许范围维护。
- 具体物品详情和生命周期时间线。
- 买入、维修和卖出记录。
- 附件预览和下载。
- 行情曲线和样本说明。
- 持有成本、日均费用、账面损益和实际损益。
- 租户业务设置。
- 登录、首次初始化和租户成员管理。
- 用户名下拉菜单中的物品类型管理、成员管理、语言、主题和退出；顶栏主导航只保留物品入口。
- 简体中文和英文代码语言包，以及账号级 `system`、`light`、`dark` 主题偏好。

### 13.2 MCP 工具

2026-09-12：用户将 `dev-mcp` 批次标为 UAT 检查点。当前 40 个工具、
本批次改动和明确限制见 `docs/MCP_RELEASE_NOTES.md`，准确工具目录见 `docs/MCP.md`。
本次同时包含型号图片的 Web 上传/导入，不包含 MCP 图片工具或以旧换新关联。
晋级以当前提交的双数据库全要素验证和打包冒烟为准，生产仍须单独批准。

当前已批准独立增量：在 `dev-mcp` 实现同进程 Streamable HTTP、现有账户
OAuth/PKCE 授权、可撤销的客户端权限和现有业务管理工具。直接复用 application
服务；管理写入补共享幂等，生命周期沿用已有回执。以本地 Codex 实测为验收，
2026-09-10 批准追加服务器端公开 HTTPS GLB 直链导入，复用既有校验、BlobStore、
资源注册及管理幂等；导入与绑定分开。限制 SSRF、DNS 重绑定、重定向、大小、
时限和并发，不接受调用者 Cookie/认证头、压缩包或任意网页解析。
仍不含 MCP 文件上传、行情、成员管理、stdio 或公网部署。完整范围与未完成清单见
`docs/MCP_IMPLEMENTATION.md`；以下示例中的附件和行情仍属于后续阶段。

当前 MCP 只暴露语义化工具，例如：

```text
search_product_models
search_specification_tags
record_event
correct_event
get_asset
list_assets
get_asset_cost
get_portfolio_summary
import_3d_resource_from_url
```

写入工具必须经过输入验证和业务规则；不提供任意 SQL 写权限。
买入、维修和卖出通过 `record_event` 加已有类型实现；事件类型是可复用行为分类，
商品/服务名称属于备注。通用证据附件尚未实现；本次开发整合增加的行情工具见 docs/MCP.md。

## 14. 建议目录结构

```text
cmd/assetloop/
internal/
├── domain/
├── application/
├── config/
├── store/
│   ├── sqlite/
│   └── postgres/
├── blob/
│   ├── local/
│   └── aliyun/
├── market/
│   ├── onebound/
│   └── manual/
├── web/
├── mcp/
└── scheduler/
migrations/
├── sqlite/
└── postgres/
web/
├── templates/
└── static/
docs/
```

## 15. 实施阶段

### 阶段 1：基础工程

状态：已完成。

- 建立 Go 模块和单二进制命令框架。
- 实现配置加载和 `.env.example`。
- 建立领域类型和租户边界。
- 建立 SQLite/PostgreSQL 初始迁移。
- 建立 Store 接口和双数据库一致性测试。

验收：两个数据库都能完成建库、升级、创建并查询最小物品记录。

### 阶段 2A：认证、RBAC 和 Web 外壳

状态：已完成。

- 首次初始化默认租户和 Owner。
- 本地账号密码、服务端 Session、退出和 CSRF。
- Owner/Editor/Viewer 应用层能力检查。
- 租户成员管理、安全审计和跨租户隔离测试。
- 服务端渲染的基础布局、导航和错误页面。
- 账号级语言与主题偏好、未登录语言匹配、角色化用户名菜单，以及服务端首屏深色模式。

验收：Owner 可以登录并管理成员；Editor 不能管理成员；Viewer 不能执行写操作；任意角色都不能越租户读取数据。

### 阶段 2B：资产目录管理

状态：已完成。

- 类别、型号、类型化标签和具体物品应用服务，支持非破坏性编辑和类别图标键。
- 首页具体物品列表、列表/卡片视图和空状态；物品与型号列表保留筛选、排序和分页。具体物品录入/编辑使用与详情一致的独立页面，选择型号和可选标签；选择变化只保留仍允许的值，移除前提示。
- 独立标签管理及型号编辑抽屉。Viewer 读取，Owner/Editor 维护；共享名称修正须明确不改变含义。
- SQLite/PostgreSQL Store 一致性和升级测试。
- 颜色及旧规格原文迁成标签，原物品和生命周期历史保持完整。已执行的 00013 不回写；成对前向迁移 00014 删除旧规格表、映射及物品 variant/color 字段，未知外键依赖中止并回滚，允许处理后重试。新 Item 只接受 model_id 与 tag_ids，旧 variant_id 参数明确拒绝，不再转换；旧规格维护和迁移确认入口已移除。SQLite 与 Web 回归通过；原 8080 已升级 00014，备份与升级后物品、全部 15 条事件、标签关联和资源元数据逐字段一致，GLB 哈希不变。
- 独立 3D 模型库支持分页搜索、上传、按需预览、来源编辑、标签/分类、引用查询及未引用资源删除/清理重试。文件固定，更换文件新建资源；多个容量可共享外观资源。
- 型号通用默认、外观规则和物品专属覆盖可选择已有资源或上传绑定。详情按“物品专属 → 最具体已确认外观默认 → 型号通用 → 图片/图标”解析；同等具体条件指向不同资源时回退并向编辑者提示，文件加载失败直接回退图片。
- GLB 来源 URL、作者和许可证作为描述信息保存；本期不含自动搜索、AI 建模、审批、版本目录、AR 或 MCP 上传。
- 具体物品的“设置 3D 资源”入口收纳在编辑页；详情页仅保留返回、编辑与模型查看控件，不重复放置绑定管理按钮。
- 3D 展示默认约两分钟旋转一圈，提供自动旋转开关；拖动时暂停、结束后继续。减少动态效果时禁用自动旋转；页面隐藏或模型离开视口时停止渲染。背景沿用用户明暗模式和主题色。
- 初始视角和重置按模型投影包围盒适配可用画布约 75%，底部预留 56px 单行控件；视窗变化保持用户缩放比例。播放/暂停与重置为图标，提示简化；桌面悬停或键盘聚焦时显示，触屏保持可见。

验收：用户建立类别、型号和允许标签，创建同颜色不同容量的物品并共享已确认外观模型；专属刻字模型覆盖后可解除恢复继承。类型/标签停用保留历史、范围收缩受引用保护、引用资源不可删除；双数据库迁移与全要素验证是 UAT 必经门槛。

### 阶段 2C：核心资产生命周期

状态：已完成。累计回归覆盖 SQLite 和 PostgreSQL；UAT 发布包已手工验证类别、型号、两个规格、具体物品、外币买入换算、维修、更正、卖出、本位币汇总和追加式审计约束。

- 交易与资产事件。
- 开发分支已补充生命周期事务内校验和请求幂等：并发买入/卖出保持唯一有效记录，Web 自动生成请求标识，同一命令重试返回原事件；更正及幂等回执原子提交。此能力已进入后续验收基线。
- 本位币、原币、汇率证据和审计；Web 选择外币并提交必填换算信息即表示确认，不再增加重复复选框。
- Agent 对话确认后直接记录正式生命周期事件。
- 买入、维修、卖出、租户自定义事件类型、作废替换和支持服务端筛选、排序、分页的生命周期时间线页面；时间线默认只展示有效事件，可按需显示已作废的原记录，但不把技术性作废事件渲染为独立记录；概览通过单次聚合查询计算全租户现金流。

验收：可以在 Web 中完整记录一件物品从买入、维修到卖出的生命周期，并正确计算本位币现金流；更正保留原始事件。

### 阶段 3：附件系统

- 状态：BlobStore、Store Registry、共享 ObjectKeyMapper、Local 与 Aliyun OSS 适配器已由型号 3D 媒体切片先行落地；通用附件元数据与迁移命令待完成。
- 实现统一 ObjectKeyMapper。
- 实现 LocalBlobStore。
- 实现 AliyunOSSBlobStore。
- 实现附件元数据、校验和下载。
- 增加 Local 到 OSS 的校验迁移命令。

验收：同一业务流程可以在不改业务代码的情况下切换附件 Store，历史附件仍可读取。

### 阶段 4：转转行情与每日快照

开发交付顺序：
1. 转转与 Frankfurter 适配器、真实脱敏样本、双库 00015 迁移与 Store、应用价格/汇率策略。
2. 共享行情管理、预览确认与物品绑定、详情来源/价格/时间展示。
3. refresh-market、系统计划任务入口、跨进程租约与停采策略、升级/全要素及界面回归。

已按用户确认的适配器币种契约启用正式写价；UAT 和生产分别等待明确授权。当前开发检查点已完成 SQLite/PostgreSQL 行情、升级与完整场景实跑；正式 UAT 仍需针对选定版本执行规定验证。

### 阶段 5：Web 和 MCP

- 完成附件和行情等剩余 Web 页面。
- 完成语义化 MCP 工具。
- 验证 Harness 截图识别、对话确认到 MCP 直接入库的完整链路。

验收：用户可以通过对话提交截图并确认字段，MCP 调用后立即在 Web 中看到正式物品和生命周期记录；需要修改时通过 Web 或 MCP 走追加式更正。

### 阶段 6：发布与 SaaS 准备

- GitHub Actions 多平台发布。
- 固化 `dev -> uat -> prod` 晋级、受保护分支和隔离打包环境。
- SQLite 升级备份和恢复验证。
- PostgreSQL 发布迁移 Job。
- Docker 镜像和健康检查。
- 加入认证、租户解析、限流和 Secret Store 后部署 SaaS。

## 16. 第一阶段完成标准

- 本地默认配置可零外部服务启动。
- SQLite 老数据升级前自动备份，升级失败不继续运行。
- PostgreSQL 与 SQLite 通过相同业务一致性测试。
- 本位币统计无浮点金额误差，原币和汇率可追溯。
- 附件可在本地和阿里云 OSS 间切换、读取和校验。
- 转转可替换，外部协议与字段仅由适配器处理。
- MCP 无裸 SQL 写入能力。
- Windows、Linux、macOS 用户无需安装 Go 或 Node 即可运行发布包。

## 设置中心整合（开发迭代）

用户菜单收敛为设置入口，物品类型、规格标签、3D 资源、生命周期类型共用
标签导航；成员管理独立，语言和主题保持快捷操作。保留现有路由、权限和
应用服务，不增加数据表。根管理页局部切换，支持独立查询状态、历史导航、
失败重试及未保存保护；3D 详情继续按需加载。沿用现有 8080 与数据库，
开发检查点不自动触发 UAT。

型号允许标签编辑采用两个联动穿梭框：标签值允许范围、类型级外观覆盖。
支持动态标签、分组搜索、行高亮多选、双击移动、中间箭头批量移动、恢复默认及手机单栏切换。
统一保存现有字段；不新增表或修改既有引用约束。

## 关联详情抽屉（本地开发批次）

累计净成本按记录数量呈现：1～5 条用紧凑日期／类型／此次后净成本列表，
超过 5 条保留按时间比例的阶梯趋势图与明细；空状态及成本计算不变。

- 物品表单用可搜索的 3D 资源选择与预览代替独立绑定入口；选择为父层草稿，
  与物品字段及标签同事务保存，空选择恢复继承。资源自身编辑仍独立保存。

- 生命周期录入仅选择类型，下拉框末项提供新增并自动选中创建结果；
  已有类型编辑统一由设置进入，不在录入表单提供管理或详情入口。

- 统一详情片段协议和抽屉栈，先覆盖标签值到标签类型，再接通型号、类别、
  物品、生命周期类型、资源绑定、外观配置和引用对象。
- 父层保留草稿、文件和滚动位置；子层独立保存，取消父层不回滚子层结果。
  关联操作不经过管理列表，不改变主页面地址。
- 使用原生多层 modal、64px/200ms 轻推移（累计最多 128px）、20px 入场与 8px/120ms 淡出、窄屏限位和减少动态效果设置；
  统一关闭确认、提交保护、请求竞态以及 3D 暂停/恢复/销毁。
- 以 SQLite HTTP 全流程、Go 回归、前端状态测试和浏览器核验作为本地证据。
  PostgreSQL 实库仍单独标记未验证；不新增迁移，不自动晋级 UAT 或生产。

## 行情与 MCP 整合（2026-09-12 开发批次）

基于已验收 MCP UAT e8ab8f9，合入 dev-market-quotes。MCP 增加 10 个行情工具：
已存行情列表/历史/物品最新价、候选搜索、规格选择、查询预览、确认创建、
改名与启停、显式绑定/解除、单序列刷新；总计 50 个语义工具。
沿用现有目录权限和幂等回执，查询规格与行情口径在确认前分别呈现，
持有成本继续只读真实生命周期现金流。通用凭证附件和行情曲线 UI 仍延期。

保留已验收 15–17 迁移，行情迁移顺延到 18–19，并支持旧行情开发库 15/16
非破坏性升级。验证同时覆盖 MCP OAuth 全流程、行情/汇率、Web 价格展示、
权限隔离、重试并发与旧数据保留。此批次先做开发整合和测试，UAT/生产仍按
项目规定分别授权；不重复要求已验收 MCP 批次重新人工验收。


### 三层权限与整件物品删除（开发批次）

固定访客、Worker、管理员三层。访客仅查看物品；Worker 可新增和修改物品、
录入与纠正生命周期，并选择已有公共定义和资源；管理员维护公共类型、型号、
标签、事件类型、图片、3D 资源、行情、成员和设置。无权限入口不渲染，Web
直访及 MCP 调用同样校验。初始账号默认管理员，新成员默认 Worker，已有角色保留。
管理员可修改成员角色，不能降级最后一名管理员，已有会话按新角色授权。

详情底部仅管理员显示永久删除入口，确认后清除物品及所属生命周期和关联；
跨物品共享交易、类型、型号、标签、型号图片、独立资源库及共享行情保留。
本次不新增通用附件系统。删除影响汇总统计，失败回滚，旧请求不能复活物品。
配对 00020 迁移仅增加受控删除通道，不在升级时清除用户数据。
开发验收覆盖三角色页面/API/MCP、角色即时生效、并发最后管理员保护、
SQLite/PostgreSQL 删除/回滚/旧版升级，以及扩展的全要素场景。当前批次不自动发布 UAT 或生产。

物品详情的删除入口改为底部全宽按钮：第一次滚到底保持隐藏，停顿后再次向下
滚动或重新上滑手势才展开。同一手势的惯性不触发展开；键盘和无 JavaScript
场景保留可访问入口。展开后仍进入原有确认页，不直接删除。
